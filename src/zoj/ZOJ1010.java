package zoj;
/**
 * ZOJ1010: Area
 * @author Jason
 * 
 */


public class ZOJ1010 {
	/**
	 * This method compute the area of a triangle.
	 * parameters are the coordinates of the 3 vertexes.
	 * method: Heron's formula.
	 * S = Math.sqrt(P*(p-a)*(p-b)*(p-c))
	 * p = (a + b + c) / 2.0
	 */
	/*static double triangleArea(HashMap<Double, Double> V) {
		//ArrayList<Double> arcs = new ArrayList<Double>();
		for(double x : V.keySet()) {
			//double y = V.get(x);
			//double arc = Math.sqrt(Math.pow(a, b))
		}
		
		return 0;
	}*/
	
	public static void main(String[] args) {
		// TODO Auto-generated method stub
	}

}
